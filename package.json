{
  "name": "img-compressor",
  "version": "1.0.0",
  "description": "This is a tool to convert images from jpeg to webp and compress them",
  "scripts": {
    "compress": "node compress.js",
    "convert": "node convert.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "sharp": "^0.32.6"
  }
}
